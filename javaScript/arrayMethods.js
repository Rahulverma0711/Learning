// Important array methods

// forEach
// map 
// filter
// reduce

// forEach

const numbers=[6,7,9,11];

function myFunc(number,index){
      console.log(`index is ${index} number is ${number}`);
      
}

// for(let i=0;i<numbers.length;i++){
//     myFunc(numbers[i],i);
